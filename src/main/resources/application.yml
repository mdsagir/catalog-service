server:
  port: 8080
  tomcat:
    connection-timeout: 5s
    threads:
      max: 100
      min-spare: 10
  error:
    include-message: always
    include-binding-errors: always
  shutdown: graceful
spring:
  application:
    name: catalog-service
  lifecycle:
    timeout-per-shutdown-phase: 15s
  config.import: "optional:configserver:http://localhost:8888"
  cloud:
    config:
      request-connect-timeout: 1000 # 1s
      request-read-timeout: 5000 # 5s
      fail-fast: false
      retry:
        max-attempts: 6
        initial-interval: 1000 # 1s
        max-interval: 2000 # 2s
        multiplier: 1.1
  profiles:
    active:
  datasource:
    username: root
    password: NdMlPpz4muoC4Gp4
    #url: jdbc:mysql://host.docker.internal:3306/springnative
    url: jdbc:mysql://34.122.53.158:3306/springnative
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
management.endpoints.web.exposure.include: refresh
